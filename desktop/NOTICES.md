# Notices

Portions adapted from LQ.AI Desktop (https://github.com/LegalQuants/lq-ai),
Copyright Kevin Keller, Apache License 2.0.

The adapted portions include the Electron + electron-vite + electron-builder
project layout, the pure-core/orchestrator/runner architecture (`src/core/*`,
`src/main/*`), the vanilla-TS renderer approach, and the macOS build/notarization
configuration (`build/entitlements.mac.plist`, `build/notarize-dmg.cjs`,
`electron-builder.yml`).
