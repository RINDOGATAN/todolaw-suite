# Per-install runtime artifacts — a git-cloned kit generates these next to the
# script; none of them ever belongs in the repo (.env holds the install's keys).
.env
backups/
logs/
portal/
.suite-config
run-transcript.txt
run-transcript2.txt

# Local working notes, not shipped
CLAUDE.md
